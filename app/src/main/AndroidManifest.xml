<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.talentsprint">

    <application
        android:name=".TalentSprintApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activities.DashboardActivity"
            android:screenOrientation="portrait">
        </activity>
        <activity
            android:name=".activities.SplashActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/app_name"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".activities.StudyMaterialActivity">
        </activity>
        <activity android:name=".activities.StudyMaterialTopicActivity">
        </activity>
        <activity android:name=".activities.StudyMaterialSubtopicActivity">
        </activity>
        <activity android:name=".activities.StudyMaterialArticlesListActivity">
        </activity>
        <activity android:name=".activities.CurrentAffairsActivity">
        </activity>
        <activity android:name=".activities.CurrentAffairsTopicsActivity">
        </activity>
        <activity android:name=".activities.TermsAndConditionsActivity">
        </activity>
    </application>

</manifest>